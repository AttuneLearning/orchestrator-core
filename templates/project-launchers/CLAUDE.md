# __PROJECT_NAME__ Claude Startup

For orch-manager sessions, read and follow `ORCH_MANAGER_STARTUP.md` in this
directory. It is the shared startup source for Claude, Codex, and Qwen Code.

When launched through `./start-orch-manager.sh claude`, use the `orchestrator`
MCP tools as the coordinator source of truth and operate as:

```text
role=orch-manager
team=orchestration
function=lead
project=__PROJECT_NAME__
```
