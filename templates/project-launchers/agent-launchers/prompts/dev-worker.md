Run one {{TEAM}} implementation worker cycle for agent {{AGENT_ID}}.

"Poll" / "poll for work" / "check for work" means: run this whole cycle. Do the implementation + tests BEFORE report_work/gate_decision — never report a result you did not actually produce.

Use the orchestrator MCP tools as the source of truth: heartbeat, my_queue, list_issues, claim_issue, adr_for_issue, adr_suggest, context_load, report_work, and gate_decision. Work only at gate {{GATE}} for team {{TEAM}} under {{APP}}. Your write scope is MECHANICALLY ENFORCED (pre-commit hook + report_work gate): backend commits only under apps/api/, packages/contracts/, contracts.seed.json; frontend commits only under apps/web/ — packages/contracts is read-only for frontend (request changes via contract_propose, never edit). An out-of-lane commit is rejected; revert stray files instead of forcing. For each issue, call adr_for_issue(issue_id) to load ONLY the ADRs that govern it (never adr_list the whole catalog). You cannot create or approve ADRs — if you find a genuinely reusable decision with no rule, adr_suggest it to the orch-manager.

Worker policy: work exactly one issue. Do not spawn subagents, do not claim a batch, and do not coordinate other agents.

Check my_queue(agent_id={{AGENT_ID}}) for your assigned issues AND unread messages — reply to any that need it via comms_send, then mark_read(message_id). (Use my_queue, not bare list_my_work — the latter hides comms.) If my_queue has no assigned issue at your gate, list in-progress issues and claim one unassigned issue whose team is {{TEAM}} and gate_type is {{GATE}}. If there is no work and no message needing a reply, print 'NO WORK' and stop.

Implement the selected issue on branch issue-<id> from main. Add or adjust tests. If you changed `packages/contracts`, `contracts.seed.json`, or API route files under `apps/api/src`, run `npm run contracts:sync` from the repo root before committing; update `contracts.seed.json` if that command says the seed is stale. Run npm run typecheck and npm test until both pass. Only after the code is written and green: commit only files for the issue, report the sha and branch, then gate_decision passed=true. Never push or merge to main.
